<UserControl
    x:Class="CalendarB.UserControls.CalendarOnContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalendarB.UserControls"
    xmlns:controls="using:CalendarB.Controls.RTDCalendarView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="108"/>
            <RowDefinition Height="385"/>
            <RowDefinition Height="56"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#ED1C24"/>
        <Border Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="2" Background="#FFFFFF"/>
        <controls:RTDCalendarView x:Name="calendar"
            Grid.Row="1"
            Grid.Column="0"
            Margin="20"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"/>
    </Grid>
</UserControl>
